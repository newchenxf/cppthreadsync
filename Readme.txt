# 说明
这是一个C++ 线程同步方案

可以把你想要的多个任务，放到同一个线程执行，任务按顺序执行，确保有序

对于需要操作同样的资源，这个方案可以让你轻松的避免资源多线程操作的冲突


# 如何编译？
如果是Linux环境，直接目录下执行
make

如果是Android NDK环境，则进入build目录，执行
ndk-build

# 如何查看日志？
main函数的intializeLog定义了你的日志路径，可以到该路径下取读取，也可以自己修改路径